{
  "name": "@neoaren/comet",
  "version": "1.0.0-beta",
  "description": "A powerful DX-first routing library for Cloudflare Workers.",
  "scripts": {
    "build": "rimraf dist && npm-run-all build:esm build:cjs build:dts",
    "build:esm": "esbuild src/index.ts --bundle --outfile=dist/index.mjs --format=esm --target=es2020",
    "build:cjs": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=cjs --target=es2020",
    "build:dts": "tsc",
    "lint": "eslint . --ext ts",
    "lint:fix": "eslint . --ext ts --fix"
  },
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "import": "./dist/index.mjs",
      "types": "./dist/index.d.ts"
    }
  },
  "repository": "github:neoaren/comet",
  "keywords": [
    "cloudflare-workers",
    "cloudflare",
    "workers",
    "routing",
    "comet",
    "router"
  ],
  "author": "NeoAren",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/NeoAren/comet/issues"
  },
  "homepage": "https://github.com/NeoAren/comet#readme",
  "dependencies": {
    "zod": "^3.21.4",
    "zod-to-json-schema": "^3.18.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "@lint-my-life/eslint-config-comet": "^0.2.2",
    "esbuild": "^0.17.18",
    "npm-run-all": "^4.1.5",
    "rimraf": "^4.4.1",
    "typescript": "^5.0.4"
  }
}
